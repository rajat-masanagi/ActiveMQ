import { useEffect, useState } from "react";
const API_URL="http://localhost:8081/orders"; const EMPTY={customerEmail:"",product:"",quantity:1};
export default function App(){const [form,setForm]=useState(EMPTY),[orders,setOrders]=useState([]),[loading,setLoading]=useState(true),[submitting,setSubmitting]=useState(false),[error,setError]=useState("");
async function load(){setLoading(true);setError("");try{const r=await fetch(API_URL);if(!r.ok)throw Error(`Could not load orders (${r.status})`);setOrders(await r.json())}catch(e){setError(e.message)}finally{setLoading(false)}}
useEffect(()=>{load()},[]); function change(e){const {name,value}=e.target;setForm({...form,[name]:name==="quantity"?Number(value):value})}
async function submit(e){e.preventDefault();setSubmitting(true);setError("");try{const r=await fetch(API_URL,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});if(!r.ok)throw Error(`Could not create order (${r.status})`);setForm(EMPTY);await load()}catch(x){setError(x.message)}finally{setSubmitting(false)}}
return <main><h1>Order lab</h1><p>Create an order and watch Email Service consume its event.</p><form onSubmit={submit}><label>Customer email<input name="customerEmail" type="email" value={form.customerEmail} onChange={change} required/></label><label>Product<input name="product" value={form.product} onChange={change} required/></label><label>Quantity<input name="quantity" type="number" min="1" value={form.quantity} onChange={change} required/></label><button disabled={submitting}>{submitting?"Creating...":"Create order"}</button></form>{error&&<p className="error">{error}</p>}<section><div className="heading-row"><h2>Orders</h2><button className="secondary" onClick={load}>Refresh</button></div>{loading?<p>Loading...</p>:orders.length===0?<p>No orders yet.</p>:<ul>{orders.map(o=><li key={o.id}><strong>{o.product}</strong> × {o.quantity}<span>{o.customerEmail}</span></li>)}</ul>}</section></main>}
